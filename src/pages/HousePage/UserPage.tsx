import React, { ReactElement } from 'react'

interface Props {
    
}

export default function UserPage({}: Props): ReactElement {
    return (
        <div>
            글 목록, 카테고리, 태그
        </div>
    )
}
